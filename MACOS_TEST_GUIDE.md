# macOS 실주행 CSV 플레이백 테스트 가이드 (macOS Telemetry CSV Playback Test Guide)

본 문서는 Windows OS 및 아이레이싱(iRacing) 게임이 실행되지 않는 **macOS 로컬 개발 환경**에서, 실제 주행 기록인 텔레메트리 CSV 파일을 사용해 투명 오버레이 HUD와 실시간 코칭 시스템을 테스트하고 검증하는 방법에 대해 설명합니다.

---

## 1. 작동 원리 (How it Works)

1. **플랫폼 감지**: 앱이 시작될 때 실행 플랫폼을 감지하며, macOS(`process.platform !== 'win32'`) 환경일 경우 자동으로 **Mock 텔레메트리 모드**로 진입합니다.
2. **CSV 스캔 및 로드**: `assets/mock/` 폴더 내에 저장된 첫 번째 `.csv` 파일을 탐색하여 데이터 내용과 파일명을 파싱합니다.
3. **60Hz 프레임 스트리밍**: 파싱된 데이터 행들을 **60Hz (약 16.6ms 간격)** 주기로 실시간 스트리밍하여 메인 프로세스와 분석기(`analyzer.js`)에 주입합니다.
4. **루프 재생**: 데이터가 파일 끝에 도달하면 다시 0번 프레임으로 되돌아가며 무한 루프 주행을 시뮬레이션합니다.
5. **폴백(Fallback)**: 폴더에 CSV 파일이 없거나 파싱에 실패할 경우, 기본 Spa-Francorchamps 트랙에 맞춰 임의의 물리 법칙으로 자동 가상 주행하는 절차적 생성기가 작동합니다.

---

## 2. 테스트 환경 준비 (Setup)

### ① 의존성 패키지 설치
터미널에서 프로젝트 루트 디렉토리로 이동하여 필요한 Node 패키지들을 빌드/설치합니다.
```bash
npm install
```

### ② 테스트 주행 CSV 파일 배치
1. 프로젝트 루트에 `assets/mock/` 디렉토리를 생성합니다. (앱 첫 구동 시 자동으로도 생성됩니다.)
2. 검증할 텔레메트리 주행 CSV 파일을 해당 폴더 안에 복사해 넣습니다.
   * *참고: 이 CSV 파일은 Windows 실제 주행 시 자동으로 문서 폴더(`~/Documents/MustBeTheApex/Telemetry/`) 하위에 저장된 파일을 그대로 가져와 사용하면 됩니다.*

#### 💡 파일명 명명 규칙 (Filename Convention)
플레이백 엔진은 파일명을 통해 트랙(서킷)과 차량명을 파싱하여 UI 및 SQLite DB 조회에 활용합니다:
```text
session_<sessionType>_<track-slug>_<car-slug>_<timestamp>.csv
```
* **예시**: `session_practice_spa-francorchamps_porsche-911-gt3-r_1780640181555.csv`
* 파일명이 위 패턴과 맞지 않는 경우 기본값(`Spa-Francorchamps`, `Porsche 911 GT3 R`)이 할당됩니다.

#### 📊 CSV 데이터 포맷 요구 사항 (CSV Schema)
CSV 파일의 첫 번째 행은 헤더여야 하며, 다음 칼럼들이 대소문자 구분 없이 콤마(`,`)로 구분되어 포함되어야 합니다:
* `SessionTime` (초 단위 소수점)
* `LapDist` (미터 단위 누적 거리)
* `Lap` (랩 번호)
* `Speed`, `Throttle`, `Brake`, `Gear`, `Steering`, `RPM`

---

## 3. 앱 실행 및 플레이백 구동

터미널에서 다음 명령어를 입력하여 일렉트론 오버레이 앱을 실행합니다:
```bash
npm start
```

### 🖥️ 터미널 콘솔 로그 확인
성공적으로 CSV 파일을 읽어 들이면 터미널에 아래와 같은 안내 로그가 출력됩니다:
```text
[Mock] Loaded CSV playback from /path/to/must-be-the-apex/assets/mock/session_practice_spa-francorchamps_porsche-911-gt3-r.csv: track="Spa Francorchamps", car="Porsche 911 Gt3 R", length=7004m, frames=18524
```
이 상태가 되면 백그라운드에서 가상의 차량이 60Hz 주기로 주행을 에뮬레이션하기 시작합니다.

---

## 4. 인게임 코칭 오버레이 기능 검증

주행이 구동되는 동안 투명 오버레이 HUD의 4가지 핵심 기능을 시각적으로 테스트할 수 있습니다.

### ① HUD 위치 및 크기 편집 (Layout Edit Mode)
1. 앱 오버레이 화면이 뜬 상태에서 글로벌 단축키 **`Command + Shift + O`**를 입력합니다.
2. 화면 상단에 붉은색 **"HUD EDIT MODE"** 배너가 켜지며 점선 형태의 위젯 테두리들이 나타납니다.
3. 이 상태에서는 마우스 클릭 관통(Click-Through)이 일시 정지되어 위젯을 조작할 수 있습니다:
   * **위치 이동**: 각 위젯 상단의 붉은색 밴드(`TARGET STATUS`, `FOCUS GUIDE`, `LIVE COACHING`)를 마우스 드래그로 이동시킵니다.
   * **크기 변경**: 각 위젯 우측 하단 모서리의 삼각형 크기 조절 손잡이를 드래그하여 크기를 늘리거나 줄입니다.
4. 배치가 끝나면 다시 **`Command + Shift + O`**를 누릅니다. 편집 배너가 사라지고 레이아웃이 잠금 상태가 되며 마우스 클릭이 관통하는 게임용 투명 오버레이 상태로 복원됩니다.
   * *설정된 좌표 정보는 브라우저 `localStorage`에 영구히 저장되어 앱 재시작 시에도 유지됩니다.*

### ② 200m 전방 Focus Guide 검출 테스트
* 차량이 코너 브레이크 포인트(`D_brake_start`) 전방 200m 안으로 진입하면, 좌상단 Focus Guide 카드에 `TURN [코너번호]`, `LEFT / RIGHT (회전 방향)`, `공략 기어 단수`, `목표 최대 제동 압력`이 표시되는지 확인합니다.
* 차량이 코너로 전진함에 따라 남은 거리 미터(m) 수치가 줄어들고 카운트다운 게이지바가 줄어드는지 확인합니다.
* 코너 에이펙스를 통과하여 코너를 벗어나면 Focus Guide 카드가 자동으로 닫히고 `FLAT OUT` 상태로 복원되는지 확인합니다.

### ③ Web Speech API 무지연 음성 안내 테스트
* 코너 브레이크 포인트 250m 전방에 도달하는 순간, 시스템 스피커를 통해 코칭 음성이 선명하게 한글로 출력되는지 확인합니다.
* **음성 예시**: *"코너 3, 기어 3단, 브레이크 78 퍼센트 준비"*
* 이전 코너 음성이 채 끝나기 전에 다음 코너 이벤트가 트리거될 경우, 지연 겹침 없이 기존 음성이 즉각 캔슬(Interrupt)되고 최신 가이드가 0ms 딜레이로 갱신 출력되는지 귀로 확인합니다.

### ④ 5단계 제동 타이밍 평가 및 에지 플래시 테스트
* **브레이킹 타이밍 피드백**: CSV 기록 중 브레이크 패널을 최초 밟기 시작한 지점의 오차 델타(m)가 하단 `LIVE COACHING` 패널에 가로축 눈금 및 등급 배지로 평가 표시되는지 확인합니다:
  * `-15m` 미만: `TOO EARLY` (노란색)
  * `-15m ~ -5m`: `EARLY` (연두색)
  * `-5m ~ +5m`: `PERFECT` (형광 녹색)
  * `+5m ~ +15m`: `LATE` (주황색)
  * `+15m` 초과: `TOO LATE` (형광 빨간색)
* **에이펙스 속도 피드백 & 화면 플래시**: 코너 중심점 통과 최저 속도가 레퍼런스 대비 오차가 발생할 때 화면 가장자리에 전체화면 글로우(Edge Flash)와 Badge 팝업이 출력되는지 확인합니다:
  * 고수 속도 대비 `+5 km/h` 초과: `OVERSPEED` (적색 화면 테두리 플래시)
  * 고수 속도 대비 `-5 km/h` 미만: `TOO SLOW` (황색 화면 테두리 플래시)
  * 편차 `5 km/h` 이내 안착: `PERFECT` (녹색 화면 테두리 플래시)

### ⑤ 랩타임 갱신 및 차세대 타겟 승급(PB Upgrade) 테스트
1. 루프 구동으로 가상의 차량이 한 랩을 완주하여 랩 번호(`Lap`)가 증가하면, 실시간 분석기가 방금 완료한 랩 타임을 계산합니다.
2. 유저의 personal best 기록보다 단 1ms라도 빠를 경우, DB에 PB 정보가 경신됩니다.
3. PB 경신 시 오버레이 화면 내 우상단 `TARGET LAP` 카드에 네온 그린 컬러의 **`TARGET UPGRADED!`** 배지가 4초간 깜빡이며, SQLite DB를 쿼리하여 "유저 PB보다 약간 빠른 상위 고스트 레퍼런스 데이터"를 메모리에 리로드하여 타겟을 한 레벨 올리는지 확인합니다.
